<view class="page">
  <view class="card">
    <view class="bold">{{projectName}}</view>
    <view class="sm muted" style="margin-top:8rpx">{{date}} · {{session.start}} – {{session.end}}</view>
    <view class="sm muted">可约剩 {{session.remaining}} / {{session.capacity}}</view>
  </view>

  <view class="card">
    <view class="field"><view class="label">称呼</view><input placeholder="如何称呼您" value="{{name}}" bindinput="onName"/></view>
    <view class="field"><view class="label">手机号（必填）</view><input type="number" placeholder="用于通知与到店核验" value="{{phone}}" bindinput="onPhone"/></view>
    <view class="field row between">
      <view class="label">人数</view>
      <view class="stepper"><button bindtap="step" data-d="-1">−</button><view class="v">{{partySize}}</view><button bindtap="step" data-d="1">+</button></view>
    </view>
    <view class="field"><view class="label">备注（选填）</view><textarea placeholder="如有特殊需求请告知店家" value="{{note}}" bindinput="onNote"/></view>
  </view>

  <button class="btn block" bindtap="submit">提交预约</button>
</view>

.field.row { display:flex; align-items:center; justify-content:space-between; }

/* 手机号：输入框 + 微信一键获取按钮 */
.phone-row { display:flex; align-items:center; gap:16rpx; }
.phone-row .phone-input { flex:1; }
.phone-row .get-phone { margin:0; flex:none; font-size:24rpx; padding:0 22rpx; height:64rpx; line-height:64rpx; }

/* 黑名单阻断态：顶部警示块 + 禁用提交按钮 */
.bl-block {
  background: rgba(163, 45, 45, .07);
  border: 1rpx solid rgba(163, 45, 45, .25);
  border-radius: 16rpx;
  padding: 28rpx 32rpx;
  margin-bottom: 20rpx;
}
.bl-block .bl-title {
  font-size: 30rpx;
  font-weight: 600;
  color: var(--danger);
}
.bl-block .bl-text {
  margin-top: 10rpx;
  font-size: 25rpx;
  line-height: 1.6;
  color: var(--ink-2);
}
.btn.blk-disabled {
  background: var(--fill);
  color: var(--ink-3);
  border-color: var(--line);
}

/* 前一天提醒授权卡 */
.notify-card { border: 1rpx solid var(--line); }
.nc-head { display:flex; align-items:center; justify-content:space-between; }
.nc-title { font-size: 28rpx; font-weight: 600; color: var(--ink); }
.nc-btn { margin:0; font-size:24rpx; padding:0 24rpx; height:60rpx; line-height:60rpx; }
.nc-text { margin-top:14rpx; font-size:24rpx; line-height:1.6; color:var(--ink-2); }

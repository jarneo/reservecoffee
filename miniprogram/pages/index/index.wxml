<view class="page">
  <!-- 首次进入：资料引导卡（微信昵称一键填 + 头像） -->
  <view wx:if="{{showProfile}}" class="profile-mask">
    <view class="profile-card">
      <view class="pc-title">完善资料，预约更轻松</view>
      <view class="pc-sub">可一键填入微信昵称，也可手动修改</view>
      <button class="avatar-btn" open-type="chooseAvatar" bindchooseavatar="onChooseAvatar">
        <image wx:if="{{pAvatar}}" class="avatar-img" src="{{pAvatar}}" mode="aspectFill"/>
        <view wx:else class="avatar-ph">头像</view>
      </button>
      <view class="field"><view class="label">称呼</view>
        <input type="nickname" placeholder="点此填入微信昵称" value="{{pName}}" bindinput="onPName"/>
      </view>
      <button class="btn block" bindtap="saveProfileCard">保存</button>
      <view class="pc-skip" bindtap="skipProfile">暂不设置</view>
    </view>
  </view>

  <view class="hero">
    <image wx:if="{{homepage.heroImageUrl}}" class="hero-img" src="{{homepage.heroImageUrl}}" mode="widthFix"/>
    <view wx:if="{{homepage.intro}}" class="intro">{{homepage.intro}}</view>
  </view>

  <view class="sec-title">预约项目</view>
  <view wx:if="{{projects.length===0}}" class="note">暂无可预约项目</view>
  <view wx:for="{{projects}}" wx:key="_id" class="card tap" bindtap="goBooking" data-id="{{item._id}}">
    <view class="bold">{{item.name}}</view>
    <view class="bookinfo">
      <text wx:if="{{item.bookStatus==='paused'}}" class="tag paused">暂停预约</text>
      <block wx:elif="{{item.bookStatus==='ok'}}">
        <text class="tag ok">可预约</text>
        <text wx:for="{{item.availableDates}}" wx:for-item="d" wx:key="ymd" class="date-chip">{{d.label}}</text>
        <text wx:if="{{item.availableCount > item.availableDates.length}}" class="more">等{{item.availableCount}}天</text>
      </block>
      <text wx:else class="tag none">暂无可约日期</text>
    </view>
    <view class="sm muted" style="margin-top:8rpx">{{item.intro}}</view>
  </view>

  <view class="sec-title" style="margin-top:36rpx">店铺菜单</view>
  <view wx:if="{{products.length===0}}" class="note">暂无在售菜品</view>
  <view wx:for="{{products}}" wx:key="_id" class="menu-card tap" bindtap="goProduct" data-id="{{item._id}}">
    <image wx:if="{{item.imageUrl}}" class="menu-img" src="{{item.imageUrl}}" mode="aspectFill"/>
    <view wx:else class="menu-img ph">暂无图</view>
    <view class="menu-info">
      <view class="row between">
        <view class="bold">{{item.name}}</view>
        <view class="menu-price">¥{{item.price}}</view>
      </view>
      <view wx:if="{{item.desc}}" class="sm muted" style="margin-top:6rpx">{{item.desc}}</view>
      <view class="sm muted" style="margin-top:10rpx">查看评价 ({{item.reviewCount}}) ›</view>
    </view>
  </view>

  <view wx:if="{{role!=='none'}}" class="btn ghost block" bindtap="goAdmin" style="margin-top:24rpx">进入管理端</view>
</view>

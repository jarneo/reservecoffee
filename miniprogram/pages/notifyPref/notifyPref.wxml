<view class="page">
  <view class="card">
    <view class="sec">订阅消息</view>
    <view class="sm muted" style="margin-top:10rpx;line-height:1.6">
      选择希望接收的微信通知。开启后，系统在对应时点通过「微信服务通知」推送给你；关闭后不再推送该类通知。
    </view>
  </view>

  <view wx:if="{{loading}}" class="note">加载中…</view>

  <view wx:else class="card">
    <view class="np-item" wx:for="{{list}}" wx:key="key">
      <view class="np-main">
        <view class="np-label">{{item.label}}</view>
        <view class="np-desc">{{item.desc}}</view>
      </view>
      <switch class="np-switch" checked="{{item.checked}}" color="#7A5230"
              data-key="{{item.key}}" bindchange="toggle"/>
    </view>
  </view>

  <view wx:if="{{!loading}}" class="row between" style="margin-top:8rpx">
    <button class="btn ghost sm" bindtap="disableAll">全部关闭</button>
    <button class="btn sm" bindtap="enableAll">全部开启</button>
  </view>

  <view wx:if="{{debug}}" class="dev-box">
    <button class="btn debug sm" bindtap="testPopup">开发调试：测试订阅弹窗</button>
    <view class="sm muted" style="margin-top:8rpx;line-height:1.6">
      调试用：一次性向微信申请全部 5 类通知授权，验证原生弹窗能否弹出。若弹窗不出现且结果为「拒绝」，
      说明已勾过「总是保持」——请用微信开发者工具「清缓存(授权数据)→重编译」后重试。
    </view>
  </view>

  <view class="sm muted" style="margin-top:24rpx;line-height:1.6">
    说明：微信订阅消息为「一次性授权」，开启后会向微信申请一次授权，使用一次后需再次开启。
    若某项未能在弹窗中允许，系统会在该时点改用短信通知你（须已填写手机号）。
  </view>
</view>

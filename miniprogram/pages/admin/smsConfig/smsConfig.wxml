<view class="page">
  <view class="sec">短信通知配置 · 仅超级管理员</view>
  <view class="card">
    <view class="field"><view class="label">短信签名 SignName</view><input value="{{signName}}" bindinput="onSign" placeholder="如：法兰绒咖啡"/></view>
    <view class="field"><view class="label">SDK AppId</view><input value="{{smsSdkAppId}}" bindinput="onApp" placeholder="如：1400000000"/></view>
    <view class="field"><view class="label">SecretId</view><input value="{{secretId}}" bindinput="onId" placeholder="{{secretIdMask?secretIdMask+'（已配置，留空不改）':'腾讯云 SecretId'}}"/></view>
    <view class="field"><view class="label">SecretKey</view><input value="{{secretKey}}" bindinput="onKey" placeholder="{{hasSecret?'已配置，留空不改':'腾讯云 SecretKey'}}"/></view>
    <view class="sec" style="margin-top:8rpx">五个短信模板 ID（在腾讯云短信控制台「模板管理」获取）</view>
    <view class="field"><view class="label">① 预定成功（2715328）</view><input value="{{tplSuccess}}" bindinput="onTplSuccess" placeholder="如：2715328"/></view>
    <view class="field"><view class="label">② 预约临近（2716156）</view><input value="{{tplApproaching}}" bindinput="onTplApproaching" placeholder="如：2716156"/></view>
    <view class="field"><view class="label">③ 预约过期（2716682）</view><input value="{{tplExpired}}" bindinput="onTplExpired" placeholder="如：2716682"/></view>
    <view class="field"><view class="label">④ 提前一天通知（2729722）</view><input value="{{tplDayBefore}}" bindinput="onTplDayBefore" placeholder="如：2729722"/></view>
    <view class="field"><view class="label">⑤ 取消通知（2729679）</view><input value="{{tplCancel}}" bindinput="onTplCancel" placeholder="如：2729679"/></view>
    <view class="field"><view class="label">通知正文模板（注意事项）</view><textarea value="{{noticeTemplate}}" bindinput="onNote" placeholder="如：到店请报预约手机号"/></view>
  </view>
  <button class="btn block" bindtap="save">保存短信配置</button>
  <view class="tip">均为「无参数模板」，短信正文由腾讯云控制台模板决定，此处仅填模板 ID。①预定成功 / ②预约临近 / ③预约过期 由预约流程触发；④提前一天通知 每天定时（时刻在「通知配置」页设置）；⑤取消通知 在取消后延迟发出，发送前会复校预约仍为已取消。各项目的「开启短信通知」开关见「项目配置 → 全局设定」。</view>
</view>

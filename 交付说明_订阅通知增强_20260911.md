# 订阅通知增强 · 交付说明（2026-09-11）

对应需求：①「微信优先降级开关」②「取消短信」③「每天 17:30 前一天提醒（微信 + 短信双通道）」。
本文档为交付与部署说明；字段/模板对照以 `docs/订阅模板对照表.md` 为准。

---

## 一、已就绪

### 1. 微信优先降级开关（`config.smsnotify.skipSmsIfWxOk`）

- **语义**：同一事件的微信订阅消息**已送达**（`errcode 0` → `sendSubscribe` 返回 `ok:true`）时不补发短信；微信未送达（`43101` 未授权 / `47003` 字段非法 / `-501001` 凭证异常 / 模板未配）时短信照发兜底。
- **关闭时**（默认 `false`）：不判断，两通道都发。
- 判定帮助函数：`cloudfunctions/_lib/index.js#shouldSkipSms(smsSw, wxRes)`。
- 落地位置：
  - `createReservation` / `reviewReservation` / `reviewAllReservations`：**先发订阅、再判短信**（顺序已调整），订阅结果落库 `reservations.wxSuccessOk`。
  - `remindReservation`：延迟补发的成功短信读 `wxSuccessOk`；临近/结束提醒、前一天提醒用当次订阅结果判断。
  - `cancelReservation`：用 `reserveCancel` 订阅结果判断，命中降级则**不写** `smsCancelAt`。
- 管理台 UI：「通知配置 → 微信优先降级（省短信）」开关。

### 2. 取消短信（模板 `2729679`）

- 触发：顾客取消 / **管理员代顾客取消**，顾客侧收到（不推管理员短信）。
- 机制：`cancelReservation` 只落**延迟计划**（`smsCancelSent=false` + `smsCancelAt=now+delay`，`cancelDelay` 默认 3 分钟可配）；
  由 `remindReservation` 定时补发，**发送前复校**该预约此刻仍为 `cancelled`（若已被恢复则跳过）。
- 精度：定时任务 15 分钟一轮，故实际延迟精度为 15 分钟（已按此设计，无需更高精度）。
- 为什么拆两步：`remindReservation` 已装短信 SDK，`cancelReservation` **不需**新增依赖 → 避免删函数重装的运维风险。
- 计划与结果落库：`cancelSmsPlan` / `smsCancelResult` / `cancelNotify`（订阅结果）。

### 3. 前一天 17:30 提醒（微信 `rQEgm5zUep1S9oGeYKYUEawhPK3rs48EQwNncx0HP04` + 短信 `2729722`）

- 目标：`date == 次日` 且 `status == 'confirmed'` 的预约；每天在 `dayBeforeAt`（默认 `17:30`，**管理台可配**，时/分选择器）之后的首次扫描发送。
- **同一顾客次日多笔预约只发最早一场**（`sessionStart` 升序，其次 `createdAt`），其余打 `dayBeforeSkipped='dup-openid'`。
- 幂等：每笔打 `dayBeforeNotified: true`；结果落库 `dayBeforeNotify`（微信，含 `fields`/`errCode`）、`dayBeforeSms`（短信）。
- 微信字段：地点=`store.name`、入场时间=场次开始、入场时长=结束−开始（`90分钟`）、温馨提示=「明天有预约哦，别忘记了。」
- **字段键已钉死**（见 §三）：常量 `DAY_BEFORE_KEY = { place:'thing2', time:'time1', duration:'thing3', tip:'thing4' }`，
  原先的 4 组候选试错逻辑与 `config.smsnotify.dayBeforeFields` 白名单**已整体删除**——键名以 MP 后台原文为准，不需要猜。
- 短信模板键 `dayBefore`（`2729722`）；订阅开关 `config.subscribe.dayBefore`。

### 4. 配置层与 UI

| 文件 | 改动 |
|---|---|
| `_lib/index.js` | `TPL.dayBefore`；`loadSmsSwitch` / `shouldSkipSms` / `wxDelivered` |
| `_lib/sms.js` | `templates` 增 `cancel` / `dayBefore`（含 env 回退分支） |
| `saveNotifyConfig` | 白名单增 `cancel`/`dayBefore` 开关、`dayBeforeAt`、`cancelDelay`、`skipSmsIfWxOk`；`SUB_KEYS` 增 `dayBefore` |
| `saveSmsConfig` / `getSmsConfig` | `tplCancel` / `tplDayBefore` |
| `miniprogram/utils/subscribe.js` | `TPLS.dayBefore`；`BOOKER_TPLS` 增至 5 个（`requestSubscribe` 自动按 3 个一组分片） |
| `notifyConfig` 页 | 降级开关卡片；前一天提醒「时:分」选择器；取消短信延迟选择器；订阅/短信开关列表各 +1 |
| `smsConfig` 页 | 新增「④ 提前一天通知（2729722）」「⑤ 取消通知（2729679）」输入框 |

### 5. 离线测试（新增，可重复跑）

```bash
node deploy/tests/remind.test.js             # 41 项：前一天去重/降级/键错只发一次/43101不重试/取消短信复校/成功短信降级/临近提醒/时间窗
node deploy/tests/cancel.test.js             # 23 项：取消短信 7 种分支 + 管理员代取消双订阅 + 越权拦截
node deploy/tests/saveNotifyConfig.test.js   # 30 项：配置保存的 upsert 分支/字段校验/越权（见 §六）
```
`deploy/tests/harness.js` 提供沙箱：假 `wx-server-sdk` + 假短信 SDK，但 `_lib` 用的是**真实共享库**。
截至本次交付：**94 项全绿**（remind 41 + cancel 23 + saveNotifyConfig 30）。

### 6. 新增脚本

`deploy/sync-lib.sh`：把 `cloudfunctions/_lib/{index,sms}.js` 同步到各函数目录的 `lib.js` / `sms.js`（部署前必须跑）。

---

## 二、已完成的部署与验证（2026-09-11 晚）

### 1. 云函数部署 ✅

经 CloudBase MCP 完成（账号级登录 + 自动绑定业务环境 `cloud1-d8g9mhgxm32d2eac6`）：

| 函数 | 云端 `ModTime` | 状态 |
|---|---|---|
| `createReservation` | 2026-09-11 22:53:55 | ✅ |
| `cancelReservation` | 2026-09-11 22:53:55 | ✅（无新增依赖，package.json 保持 `wx-server-sdk` 单依赖） |
| `reviewReservation` | 2026-09-11 22:54:02 | ✅ |
| `reviewAllReservations` | 2026-09-11 22:54:02 | ✅ |
| `getSmsConfig` / `saveSmsConfig` | 2026-09-11 22:54:09 | ✅ |
| `remindReservation` | **2026-09-11 23:17:01** | ✅（钉死字段键版，最终态） |
| `saveNotifyConfig` | **2026-09-11 23:17:13** | ✅（已移除 `dayBeforeFields`） |

**关键验证**（23:00 定时轮次 CLS 日志）：
- `request_source: "TCB_API"` → **MCP 更新代码后，DevTools 注册的云侧调度依然存活**（没有把调度改坏）。
- 返回体 `{"checked":3,"sentSuccess":0,"sentDayBefore":0,"sentDayBeforeSms":0,"sentCancel":0,...}` → **含新字段＝新代码已生效**，且 `sentDayBefore:0` → 时间窗正确拦下了「部署当天已过时刻」的补发，**没有在 23:00 打扰顾客，也没有误标 `dayBeforeNotified`**。

### 2. 云端配置已写入 ✅

- `config_sms.templates` 增：`dayBefore: "2729722"`、`cancel: "2729679"`（原有 success/approaching/expired 未动）。
- 新建 `config` 文档 `smsnotify`，显式落默认值：`success/approaching/expired/dayBefore/cancel = true`、`dayBeforeAt = "17:30"`、`cancelDelay = 3`、`skipSmsIfWxOk = false`（此前该文档不存在，全靠代码缺省；现在管理台可读到实际值）。

### 3. 发送时间窗（新增保护，防深夜打扰）

前一天提醒**仅在 `[dayBeforeAt, dayBeforeAt + 180 分钟]` 内触发**（`DAY_BEFORE_WINDOW_MIN`，可用 `config.smsnotify.dayBeforeWindow` 覆盖）。
- 原因①：定时任务长时间停用后恢复，不至于在深夜补发「明天有预约哦」。
- 原因②：避免部署当天（已过时刻）立刻补发一批。
- 窗口内新建的预约仍会在下一轮（≤15 分钟）被覆盖；当天错过窗口即不再补发（下单时已收到「预约成功」卡片）。

### 4. 真机端到端验证 ✅（23:30 轮次，实测）

为当晚拿到真机证据，把 `dayBeforeAt` 临时前移到 `23:25`（时间窗只覆盖 23:25–23:59），触发后**已回退为 `17:30`**。

| 观测项 | 实测值 | 结论 |
|---|---|---|
| CLS `request_source` | `TCB_API` | 云侧调度健康 |
| `[dayBefore]` 调试日志 | `target= 2026-09-12 rows= 3 picked= 1 at= 1410` | 次日筛选正确；**3 笔去重→只发 1 笔**（最早 10:00） |
| 微信订阅结果 | `errCode 43101`，`templateId rQEgm5zUep1S9oGeYKYUEawhPK3rs48EQwNncx0HP04` | 模板 ID 与 MP 后台一致；`43101`＝**用户尚未授权该模板**（非字段错、非凭证错） |
| 短信兜底结果 | `SendStatusSet[0].Code = "Ok"`，`PhoneNumber = +86180623062` | **短信真实送达**（模板 `2729722`） |
| 另两笔同一顾客预约 | `dayBeforeNotified: true` + `dayBeforeSkipped: "dup-openid"` | 未重复打扰 |
| 当日已取消那笔 | 无任何 dayBefore 字段写入 | 状态过滤正确 |
| `config.smsnotify.dayBeforeAt` | 已回退 `17:30` | 生产时刻恢复 |

**顺带查实（重要）**：本次微信发送返回的是 `43101`（微信侧「用户未授权」）而非 `-501001`——**说明云调用凭证当前有效**，网关拿到了 token 且请求被微信受理。今日早前 `reservations.reminderNotify` 记录仍是 `-501001`，说明凭证阻塞是在今日内解除的。建议明天用一笔真实预约复测「临近提醒」做最终确认。

---

## 三、字段键已确认（2026-09-11 晚）

你提供了 MP 后台「详细内容」原文，前一天模板 = **「入场时间通知」模板编号 22555**（类目：餐厅排队）：

| 后台原文 | 键名 | 代码填值 |
|---|---|---|
| 地点 | `thing2` | 店铺名「二曜路8号咖啡和清酒」 |
| 入场时间 | `time1` | 场次开始时间 `2026-09-13 10:00` |
| 入场时长 | `thing3` | 结束 − 开始，如 `90分钟` |
| 温馨提醒 | `thing4` | 「明天有预约哦，别忘记了。」 |

已据此**删除 4 组候选试错逻辑**，改为钉死常量 `DAY_BEFORE_KEY`（`remindReservation`），并同步移除 `config.smsnotify.dayBeforeFields` 白名单字段 —— 键名确定后不再需要自愈猜测，代码更简单、行为可预期。

> 重要：该模板的编号规律与既有模板**不重合**（`reminder` 是 thing10/time1/thing5），不可类推。后台若改模板，必须同步这个常量。
> 已按要求重新部署 `remindReservation`、`saveNotifyConfig`；离线测试更新为 41 项，全绿。

---

## 四、待你操作

### 1. 前端上传 ✅（你已完成）
你在微信开发者工具点了「上传」＝ 体验版已含新的管理端 UI 与 `subscribe.js`（`BOOKER_TPLS` 增 `dayBefore`）。
自检：微信打开体验版 → 管理台 → 通知配置，应能看到「微信优先降级（省短信）」开关 + 「前一天提醒」的时/分选择器。
（`deploy/sync.js` 只是另一种上传方式，你用 DevTools 上传是等效且更完整的做法——它还会注册 `permissions.openapi`。）

### 2. 授权 `dayBefore`（**现在唯一未闭环项**）
现有 3 笔 09-12 的预约已打 `dayBeforeNotified`，按设计不会再补发，所以今晚那笔只验证到「短信 + 去重 + 时刻」。
要看到「入场时间通知」**卡片本身**，请在**新版体验版（6.6.4）**里再提交一笔**未来日期**的预约：
- 提交时会弹**两次**授权框（5 个模板按 3+2 分片），**两个都要点「允许」**，其中第二个含「入场时间通知」。
- ⚠️ 若弹窗没出现，或曾勾选过「总是保持以上选择」并选了拒绝：微信里长按本小程序 → 移除，再重新进入提交。
- 之后不必等 17:30——预约一旦落在「次日」，只要还在发送时间窗内，下一轮（≤15 分钟）就会发。

### 3. 今晚的验证结果 ✅（已完成，含回退）
23:30 轮次实测：订阅走了正确的模板、返回 `43101`（未授权），**短信 `2729722` 真实送达 180****3062**，同顾客 3 笔去重为 1 笔、其余标 `dup-openid`，已取消那笔未被打扰。
临时时刻 `23:25` **已回退为 `17:30`**（`config/smsnotify` 已复核）。

### 4. 默认值（如需调整告诉我）
- 降级开关默认**关闭**（双通道都发）；卡片稳定后可开启省短信。
- 前一天时刻 `17:30`、时间窗 180 分钟、取消短信延迟 3 分钟。

---

## 五、验收点与时间预期

| 事项 | 状态 / 首次生效时间 |
|---|---|
| 前一天提醒（09-13 的预约） | 明天 **09-12 17:30** 起，此后每天例行 |
| 今晚真机验证（短信+去重+时刻） | ✅ 已完成（23:30 轮次），时刻已回退 |
| 前一天提醒的**微信卡片** | ⏳ 待你在新体验版提交一笔预约授权后即可见 |
| 取消短信 | 立刻可用：取消一笔预约 → 下一轮定时（≤15 分钟）补发 |
| 微信优先降级 | 立刻可用：管理台开开关后，微信送达即不发短信 |

排查权威入口：`reservations` 文档上的 `dayBeforeNotify` / `dayBeforeSms` / `smsCancelResult` / `cancelSmsPlan` / `reminderNotify` / `reminderEndNotify` / `wxSuccessOk`（均含 `errCode`）。
**不要**据「用户说收到了」反推通道——用户口中的「服务通知」可能是短信。

---

## 六、附带修复：全局通知配置「保存」报 -504002（2026-09-11 深夜）

**现象**：管理员 → 全局通知配置 → 点保存 → 客户端报 `-504002`（云函数执行失败）。

**真凶（CLS 日志实锤，RequestId `a81f9602-…`）**：
```
Error: document.set:fail -501007 invalid parameters. 不能更新_id的值
```
`saveNotifyConfig` 里 3 处「文档不存在就新建」的分支写成了
`set({ data: Object.assign({ _id: 'subscribe' }, patch) })` —— **`set()` 的 data 不允许带 `_id`**（文档 id 由 `doc(id)` 决定），SDK 直接抛错；函数整体抛出 → 客户端只看到笼统的 `-504002`。

**为什么只有 subscribe 爆**：`config` 集合只有 `wxAppSecret / wxAccessToken / store / mp / smsnotify` 五个文档，**`subscribe` 文档从来不存在**，所以每次保存都必然走到 set 分支并失败（`mp`/`smsnotify` 存在，走 update 本就正常）。也就是说**这个 bug 一直存在**，此前任何一次「全局通知配置保存」都没真正落地过。

**修复**：抽出 `upsertConfig(id, patch)` 统一处理「存在即 update、不存在即 set」，且 set 的 data **不带 `_id`**；同时把 `update`/`set` 带 `_id` 的约束写进离线测试桩，防止复发。返回值增加 `wrote: ['subscribe','mp','smsnotify']` 便于日后排查。

**验证**：
- 新增 `deploy/tests/saveNotifyConfig.test.js`：**30 项全绿**（首次保存走 set 新建 / 二次保存走 update 且不抹其他字段 / 空对象不炸 / mp 与 smsnotify 缺失也能建 / 非法值与越权拦截）。
- `deploy/tests/harness.js` 的假 SDK 补上真实约束：`set`/`update` 的 data 带 `_id` 抛 `-501007`（`add` 仍允许自定义 `_id`，与线上一致）。已实测：**旧写法会被桩拦下**，证明这是有效回归网。
- 全量回归 **94 项全绿**（saveNotifyConfig 30 + remind 41 + cancel 23）。
- 已重新部署 `saveNotifyConfig`（`ModTime 2026-09-11 23:44:42`、`Status Active`、`CodeInfo` 已是含 `upsertConfig` 的新代码）；MCP 冒烟调用返回 `{"code":-1,"message":"仅超级管理员可配置"}`（4ms，无异常）。

**待你确认**：请在管理台**再点一次保存**。预期：提示保存成功，且云端 `config` 集合首次出现 `subscribe` 文档（8 个订阅开关 + `texts`）。此前你在该页做的任何开关改动都没保存过，这次会以页面当前状态首次落库。

> 顺带纠正一条旧结论：`-504002` 不等于「初始化崩溃 / 依赖损坏」，它是**云函数执行期间抛出未捕获异常**的客户端统一映射；查因请看 CLS 里的真实 stackTrace（本例就是这么定位的）。

---

## 七、修复：「我的预约」里前一天的预约不过期（2026-09-12 凌晨）

**现象**：顾客端「我的预约」里，09-11 的预约仍显示「预约成功」，不显示「已过期」。

**真凶：时区（UTC 容器 × 北京时间字符串）**

`listMyReservations.effStatus` 用的是：
```js
const end = new Date(`${r.date} ${r.sessionEnd || '23:59'}`)   // ❌ 按容器本地时区解析
if (end < new Date()) return 'expired'
```
云函数容器时区是 **UTC**，而 `date`/`sessionEnd` 存的是**北京时间**，于是「北京 09-11 20:00 结束」被解析成「北京 09-12 04:00」→ **过期判定整体延后 8 小时**。实测（现在＝北京 09-12 00:07）：

| 09-11 场次 | 旧逻辑 | 正确结果 |
|---|---|---|
| 10:00–11:30 / 12:00–13:30 / 13:30–15:00 | expired | expired |
| **15:00–16:30 / 16:30–18:00 / 18:00–20:00 / 20:00–22:00** | **confirmed ❌** | expired |

**修复**：改用与 `remindReservation` 同源的算法 `Date.UTC(y, m-1, d, H, M) - 8h` 还原真实时刻（`bjTs()`），判定彻底与容器时区无关；解析失败时保守返回原状态，不误判过期。

**顺带挖出并修掉的第二、第三个缺陷**（同一批 09-11 数据暴露的）：

1. **`ended` 永不落库**（更严重）：`remindReservation` 的 pass ④ 查询写了
   `where({ status:'confirmed', reminded: _.neq(true), ended: _.neq(true) })`——
   而正常预约**都会先收到「开场前提醒」**（`reminded=true`），加上这条过滤后它们**从此不再进入循环**，
   循环里那个「已结束 → 打 `ended` + 发结束提醒」的分支永远走不到。
   **后果：结束提醒与过期短信实际上几乎从未发出过**，顾客端也一直停在「预约成功」。
   实测复现：一笔昨天 20:00–22:00 的预约（`reminded=true`）跑完 `checked = 0`、`ended` 仍是 `undefined`。
   证据也吻合线上数据：09-11 有 6 笔 `reminded=true` 却都没有 `ended`（仅 1 笔因首轮扫描时已过结束时刻才拿到 `ended`）。
   **修复**：where 只按 `ended` 过滤；重复发送改由循环内 `!r.reminded` 守卫。
2. **结束提醒会深夜补发**：修好 ①之后，历史积压会在下一轮（00:15）被批量补发「预约已完成」，对顾客是骚扰。
   新增发送时间窗 `END_SEND_WINDOW_MIN = 120`（同 `DAY_BEFORE_WINDOW_MIN` 的设计取向）：超窗只打 `ended` + 记 `reminderEndSkipped:'too-late'`，**不发消息**。
3. **`expiredWhen` 缺省口径**：后端写成 `=== 'after' ? 'after' : 'before'`（缺省 `before`），而管理台默认是「结束后」——
   云端 `config.smsnotify` 里恰好没有 `expiredWhen` 字段，于是结束提醒一直在**结束前 5 分钟**就发（顾客还坐着就收到「预约已完成」）。已改为缺省 `'after'`。

**验证与回归**：
- 新增 `deploy/tests/listMyReservations.test.js`：**18 项**（昨天早晚场必须过期 / 明天保持原状态 / cancelled·completed 优先 / 缺 `sessionEnd` 与非法值边界 / 项目名关联 / 越权过滤）。
  该文件在 **`TZ=UTC` 与 `TZ=Asia/Shanghai` 下各跑一遍都全绿**（旧代码在 UTC 下必挂，故这是有效回归网）。
- `remind.test.js` 增 **S8–S11**（共 59 项）：已 `reminded` 的预约结束后必须打 `ended`｜超窗只打标不发送｜窗口内正常发结束提醒+过期短信｜`reminded` 不重复发临近提醒｜`expiredWhen` 缺省＝结束后（结束前 3 分钟不得提前触发）。
- 全量回归 **130 项全绿**（remind 59 + cancel 23 + saveNotifyConfig 30 + listMyReservations 18），UTC 与北京时区下一致。
- 已部署：`listMyReservations`（00:11:47）、`remindReservation`（00:11:54），均 `Active`。
  下一轮定时（00:15）会给 09-11 的积压补打 `ended` + `reminderEndSkipped:'too-late'`，**不发送任何消息**。

> 顾客端**不需要重新上传小程序**：状态由云函数返回，页面只做文案映射。

---

## 八、同一时区缺陷的收口：全仓库扫描 + `effStatus` 上收共享库（2026-09-12 00:20）

§七 修完之后做了一次**全仓库扫描**，确认还有没有第三处复制粘贴的过期判定：

```
grep 'new Date(`\${' cloudfunctions/*/index.js
→ cloudfunctions/cancelReservation/index.js:28
```

**确实是同一个 bug 的第二份拷贝**（`cancelReservation` 的「该预约当前不可取消」守卫）：
```js
const end = new Date(`${r.date} ${r.sessionEnd || '23:59'}`)   // ❌ 同样按 UTC 解析
```
**后果**：顾客可在场次结束后的 **8 小时内**，取消一笔其实已经结束的预约，并把名额错误地释放回场次。
实测（TZ=UTC，北京时间 00:20）——结束于北京 **00:19**（1 分钟前）的场次：

| 实现 | 判定「已结束」？ | 偏差 |
|---|---|---|
| 旧（`new Date(str)`） | **false ❌** | **+8.0 小时** |
| 新（`Date.UTC(...) - 8h`） | true ✅ | −1.4 分钟 |

**这一轮的做法（治本，避免第三份拷贝再长出来）**：
1. `bjTs()` 与 `effStatus()` **上收到共享库** `cloudfunctions/_lib/index.js`（含完整时区注释），并加入 `module.exports`；
2. `cancelReservation`、`listMyReservations` 改为 `require('./lib').effStatus`，**删掉各自的本地实现**——此后全项目只有一份过期判定；
3. 跑 `bash deploy/sync-lib.sh`（`lib.js=64 / sms.js=5`）把共享库刷进各函数目录。

**回归**：`cancel.test.js` 增 **C10–C14**（共 32 项）——按北京时间已结束 2 小时／刚结束 1 分钟 → 必须拒绝取消且不得改状态、不得发订阅；还要 1 小时才结束 → 必须允许（防过度矫偏）；缺 `sessionEnd` 按当日 23:59；跨天场景（昨日 22:00 结束）。
> C10/C14 在 `TZ=UTC` 下是**有效回归网**（旧代码必挂）；`TZ=Asia/Shanghai` 下旧代码也会过，故必须两个时区都跑。

**全量结果**：**139 项全绿**（remind 59 + cancel 32 + saveNotifyConfig 30 + listMyReservations 18），`TZ=UTC` 与 `TZ=Asia/Shanghai` 一致。

**部署**：`cancelReservation`（00:20:35）、`listMyReservations`（00:20:44），均 `Active`，CodeInfo 已核对为共享库版本。

**⚠️ 一个需要注意的点**：`cancelReservation` 的 `config.json` 声明了 `permissions.openapi`（`subscribeMessage.send`）。
云调用权限的注册走的是微信开发者工具「上传并部署」，MCP/SCF 只换代码包、不重注册该权限。
因此**建议**在 DevTools 里对 `cancelReservation` 再走一次「上传并部署：云端安装依赖」，
避免取消订阅通知因权限未注册而静默失败（不影响其它函数，`listMyReservations` 不调 `openapi`）。
> ✅ **已完成（2026-09-12 00:25:17）**：你已上传并部署，`ModTime` 已从 00:20:35 推进到 00:25:17。
> 同时拿到反证：`createReservation` 22:53 经 MCP 部署后，00:23 仍成功发出订阅（`ok:true`）
> → **MCP 部署不会清除已注册的云调用权限**，只会「无法首次注册」。故 `remindReservation` 等无需重传。

---

## 九、管理员推送「消失」 + 管理端顾客字段不展示（2026-09-12 00:26–00:45）

### 9.1 管理员推送「没有了」= 一次性订阅授权被用尽（**不是代码 bug**）

**权威判据**（`reservations.adminNotify[].errCode`，非"用户说没收到"）：

| 批次 | `adminNew`（AJ8iC…） | `adminReview`（UQJ5…） |
|---|---|---|
| 较早（offset 25–32） | 混合，**你的 openid `o4VIyxnS…` 几乎每次 `ok:true`** | — |
| 最近两笔（00:22 / 00:23） | **5/5 全部 `43101`** | 4–5 个 `ok:true` |

`43101 = user refuse to accept the msg`，即**未订阅 / 授权已耗尽**。注意同一时刻两个模板一个通一个不通——**各模板独立计数**。

> ⚠️ **措辞澄清（重要，勿再误读）**：`43101` 的中文文案虽是「用户拒绝接收消息」，但它**只表示该模板当前没有可用的一次性授权次数**，
> **并不代表用户点过微信弹窗里的「拒绝」**。一次性授权是「发一条消耗一条」——配额用尽时后端拿到的就是这个码。
> 排查与汇报时**不要**把错误码文案当字面意思转述给用户（本项目 2026-09-12 已因此措辞不准确被用户纠正）。

**已逐一排除**（避免误判）：
- 开关：`config.subscribe` 已被你的保存创建，8 个键**全为 `true`**；`subOn` 缺省也是开（`!== false`）。
- 模板 ID：`miniprogram/utils/subscribe.js` ↔ `_lib.TPL` 逐字一致，`adminNew` 自 v6.5.0 起未变更。
- 云调用权限：`createReservation` 在 00:23 成功发送，证明网关与凭证正常（否则是 `-604101`/`-501001`）。

**机制**：微信订阅是**一次性**的——发一条消耗一条。用尽后后端只拿到 `43101` 并静默记库，管理员侧完全无感，于是表现为「以前收得到，现在收不到」。

**真实缺陷（已修）**：`util.requestSubscribe` 完全忽略授权结果，`hub.enableAdminNotify` **无条件** toast「已授权管理推送」——管理员永远没有机会发现授权失败。

**修法**：
1. `utils/util.js#requestSubscribe` → 返回 `Promise<{total, accepted[], rejected[], failed[], errCode}>`。
   ⚠️ **所有分片必须在同一个同步 tick 内下发**：微信要求 `requestSubscribeMessage` 在用户 TAP 手势上下文里调用，
   若为拿结果改成 Promise 串行下发，**第 2 组起会脱离手势而弹窗失败**（5 个顾客侧模板就会只授权到 3 个）。故同步 `forEach` 全下发，结果异步汇总后 resolve。
2. `hub.js` 按真实结果反馈：全通过 → 「已续订 N/N」；有拒绝/失败 → 弹窗说明并提示勾选「总是保持以上选择，不再询问」。
3. `hub.wxml` 授权卡片**置顶常驻**（原本是最底部一个小按钮，极易忽略）；`hub.wxss` 新增 `.notify-card`（品牌色描边）。

### 9.2 管理端看不到「备注 / 微信 / 性别 / 年龄」

**根因两处**：
- **后端**：`listReviews`（审核页数据源）只返回 `note`，**漏了 `wechat/gender/age`**（`listSessionReservations` 是齐的）。
- **前端**：`sessions.wxml`（预约管理）只在**详情弹层**渲染 `note`，**列表行完全没有**；`wechat/gender/age` 两个页面都没渲染。

> 参照反例：`pages/admin/view/view.wxml` 一直有这套渲染（微信号/性别/年龄）→ 说明只是「页面没跟上」，不是设计取舍。

**修法**：
- `cloudfunctions/listReviews/index.js` 补 `wechat/gender/age`（与 `listSessionReservations` 对齐）。
- `sessions.wxml`：列表行补「微信 / 性别·年龄 / 备注」；详情弹层把备注改为多行品牌色高亮，并新增**微信号（带「复制」按钮）** / 性别 / 年龄。
- `sessions.js` + `sessions.wxss`：新增 `copyWechat` 与 `.copy-hint`（管理员常需复制微信号去加好友联系）。
- `review.wxml`：补微信 / 性别·年龄（备注改品牌色，便于审核时判读）。

### 9.3 测试与部署

- 新增 `deploy/tests/listReviews.test.js`（**16 项**），专门锁住**前后端字段契约**：
  R2 断言四个字段必须透出（防再次漏字段），R3 断言字段缺失时返回**空串**（前端 `wx:if` 才安全），R1/R4/R5 覆盖权限、黑名单、状态过滤。
- 全量回归 **155 项全绿**（remind 59 + cancel 32 + saveNotifyConfig 30 + listMyReservations 18 + listReviews 16），`TZ=UTC` 与 `TZ=Asia/Shanghai` 一致。
- 部署：`listReviews` 经 MCP 更新；`cancelReservation` 由你在 DevTools 部署（00:25:17）。

### 9.4 待你操作

1. **续订管理推送**：进管理台点「续订管理推送」，弹窗里**全部勾选**并勾选「总是保持以上选择，不再询问」→ 之后长期生效，不再反复失效。
2. **重新上传小程序**：本轮改了 `utils/util.js`、`pages/admin/hub/*`、`pages/admin/sessions/*`、`pages/admin/review/review.wxml`，需在 DevTools 重新上传才能看到新字段与新卡片。
3. 上传后可在「预约管理 → 本场预约人」与「审核」页确认：微信 / 性别·年龄 / 备注 均已展示，微信号可直接复制。

---

## 十、审核通过后不再通知管理员（2026-09-12 00:39–00:48）

> 🔄 **本节已被 §十二 取代**：00:52 用户追加「审核拒绝，停推」→ 现在**通过/拒绝都不再通知管理员**。
> 本节保留为变更过程记录；**以 §十二 为当前实现**。

**需求**：「审核通过之后，管理员不用推送微信订阅消息」。

**原实现**：`reviewReservation`（单条）与 `reviewAllReservations`（批量）在审核后**不论通过/拒绝**都会给管理员推
`TPL.adminReview`（`UQJ5…`「待审核提醒」）并落库 `reservations.adminNotifyReview`。两个问题：
1. 审核是管理员本人的操作，再来一条推送纯属打扰；
2. 用「**待审核**提醒」模板播报「**已通过**」语义不符。

**改法**：**仅 `decision === 'reject'` 才推送**。`approve` 分支改为落库
```js
adminNotifyReview = { skipped: true, reason: 'admin push disabled on approve' }
```
保留可排查痕迹（与 `cancelSmsPlan.reason`、`reminderEndSkipped` 同一约定，避免以后又出现「为什么没收到管理推送」的无据可查）。

> ⚠️ 两个文件是「单条 vs 批量」的**同源拷贝**（文件头本就写着「单条审核逻辑与 reviewReservation 保持同步」），**必须同步改**。

**测试**：新增 `deploy/tests/review.test.js`（**23 项**），用**一个文件同时跑两个入口**、断言同一契约：

| 场景 | 断言 |
|---|---|
| 单条·通过 | 顾客收到 1 条「预约成功」；**管理员 0 条**；落库 `skipped`；状态 approved/confirmed |
| 单条·拒绝 | 管理员收到 1 条；顾客不发；状态 rejected/cancelled；**名额已释放** |
| 批量·通过 | 逐条给顾客；**管理员 0 条** |
| 批量·拒绝 | 管理员逐条收到 |
| 非管理员 | 两个入口都拒绝、零订阅发出 |
| 已处理过 | 不重复处理 |

**反证（确认测试有效）**：把 `approve` 分支临时改回旧条件跑测试 → **2 项变红**（「管理员 0 条」「落库 skipped 原因」），还原后 23/23 绿。

**顺手补的测试桩缺口**：`deploy/tests/harness.js` 的链式查询**缺 `skip(n)`**。`reviewAllReservations` 的 `loadBlacklist()` 用
`where(...).skip(n).limit(100).get()`，而 `.skip` 在 `.get()` **之前**就同步抛 `TypeError` → `.catch()` 根本没挂上 → 整个 `main` reject。
这就是「批量审核这类函数一写测试就崩」的真因。补 `skip` 后批量路径才可测，也让后续任何分页函数（导出等）都能进离线测试。

**部署**：`reviewReservation`（00:41:15）、`reviewAllReservations`（00:41:22），均 `Active`，CodeInfo 已核对含新分支。

**全量回归：178 项全绿**（remind 59 + cancel 32 + saveNotifyConfig 30 + listMyReservations 18 + listReviews 16 + **review 23**），`TZ=UTC` 与 `TZ=Asia/Shanghai` 一致。

**❓一个待你定的小点** → **✅ 已定案（见 §十二）**：00:52 用户回复「审核拒绝，停推。审核通过，推送预约成功的通知。」
即**拒绝也一并停推**，通过则只给顾客推「预约成功」。

> 补充：本修复部署于 **00:41:15**，而用户观察到的那次「通过后仍收到待审核提醒」发生在 **00:38:48**——**早于修复 2.5 分钟**，
> 因此现象与修复不矛盾；但修复**尚未被真机复验**（此后无新审核动作）。下次审核通过时留意：管理员侧应**零推送**，
> `reservations.adminNotifyReview` 应落 `{skipped:true}`。

---

## 十一、复核「我没有点拒绝」——查证结论（2026-09-12 00:45–00:55）

用户质疑上一轮的判断。逐项回查数据库，**结论：用户说得对，是我把错误码文案当字面意思误读了**。

| # | 查证项 | 结果 |
|---|---|---|
| 1 | `reservations` 中 `review:'rejected'` 记录数 | **0 条** → 从未发生过拒绝，用户「没点过拒绝」字面成立 |
| 2 | 新预约提醒是否恢复 | ✅ **`adminNew` 在 00:37:57 成功发给用户本人**（`ok:true`，doc `b9a701d46aa42e65083c433f32c603bd`）→ 续订后恢复，「一次性授权耗尽」诊断得到正向验证 |
| 3 | 投诉的那条从哪来 | 用户 **00:37:34** 下单（待审）→ **00:38:48 点「通过」** → 旧版 `reviewReservation` 随即发出**第二条**「待审核提醒」（`adminNotifyReview.ok:true`）。即用户看到的那条 |
| 4 | 修复时序 | 修复部署 00:41:15，**晚于**该次审核 2.5 分钟 → 现象与修复不冲突，但**修复尚未被真实验证** |
| 5 | 其它 4 位管理员 | `o4VIyxkS…`/`o4VIyxiD…`/`o4VIyxsL…`/`o4VIyxr1…` 长期全部 `43101` → **从未授权过**。若非真实管理员，建议在「管理员管理」清理，减少日志噪音 |

**结论与自省**：
- ❌ 我此前把 `43101` 描述为「用户拒绝/未订阅」——**措辞不准确**。正确含义 = 该模板**无可用一次性授权次数**（详见 §9.1 澄清框）。
- ✅ 我的机制诊断（授权耗尽）方向正确，且已被 00:37:57 的 `ok:true` 正向验证；**错的只是措辞**。
- ✅ 用户观察到的「通过后仍收到提醒」是**真实存在的缺陷**，已由 §十 修复；但该修复需在下一次审核时复验。
- 顺带确认：新数据的 `note`/`wechat`/`gender`/`age` 均已入库（"带狗，带轮子" / 微信号 / 性别 / 年龄）→ §9.2 的字段展示修复能显示到真实内容。





---

## 十二、审核拒绝也停推 + 通过只给顾客推「预约成功」（2026-09-12 00:52 定案）【当前实现】

**需求**（用户原话）：「审核拒绝，停推。审核通过，推送预约成功的通知。」

**定案后的完整语义**：

| 审核动作 | 顾客（小程序订阅） | 顾客（短信） | 管理员（订阅） |
|---|---|---|---|
| **通过** `approve` | ✅ 1 条「预约成功」（`reserveSuccess`） | 按开关（`smsnotify.success` + 项目 `smsEnabled`） | ❌ **不再推送** |
| **拒绝** `reject` | ❌ 不发（顾客在「我的预约」看状态） | ❌ 不发 | ❌ **不再推送** |

> 也就是说：**「待审核提醒」只在【顾客下单时】由 `createReservation` 发出**（`needReview` 分支，语义正确）；
> **审核环节本身不再产生任何面向管理员的消息** —— 审核是管理员自己的操作，再推一条纯属打扰。

**代码改法**（单条 `reviewReservation` / 批量 `reviewAllReservations` **两文件必须同步**）：

```js
// 审核结果 → 管理员推送：**「通过」与「拒绝」都不再通知管理员**（2026-09-12 按需求移除）。
// 理由：① 审核是管理员本人的操作，再来一条推送纯属打扰；
//       ② adminReview 模板是「待审核提醒」，用它播报「已通过 / 已拒绝」语义不符；
//       ③ 「待审核提醒」在【下单时】已由 createReservation 发出（needReview 分支），审核环节无需再发。
const adminNotifyReview = { skipped: true, decision, reason: 'admin push disabled on review' }
try { await db.collection(COL.reservations).doc(reservationId).update({ data: { adminNotifyReview } }) } catch (e) {}
```

- `decision` 一并落库，便于事后区分「通过而没推」和「拒绝而没推」。
- 保留 `skipped` 痕迹是刻意设计（与 `cancelSmsPlan.reason`、`reminderEndSkipped` 同一约定）——避免日后又出现「为什么没收到管理推送」却无据可查。
- `TPL.adminReview` **模板与授权不删**：`createReservation` 的下单待审提醒仍在用它；管理员侧 `ADMIN_TPLS` 授权也保持不变（模板本身仍有用）。

**测试**：`deploy/tests/review.test.js` 重写为 **26 项**，一个文件同时跑**单条 + 批量**两个入口、断言同一契约：

| 用例 | 断言 |
|---|---|
| V1 单条·通过 | 顾客 1 条预约成功；**管理员 0 条**；落库 `{skipped:true, decision:'approve', reason}` |
| V2 单条·拒绝 | **管理员 0 条**（本轮新契约）；顾客 0 条；状态 rejected/cancelled；名额 2→0 释放；落库 `decision:'reject'` |
| V3 批量·通过 | 顾客逐条收到 2 条；管理员 0 条；两条都落 skipped |
| V4 批量·拒绝 | 管理员 0 条；顾客 0 条；两条都落 `decision:'reject'` |
| V5 非管理员 | 两个入口都拒绝，零订阅发出 |
| V6 已处理 | 不重复处理 |

**反证（确认测试有效）**：临时把 V2 的旧行为（拒绝时 `notifyAdmins` 推 `TPL.adminReview`）打回 `reviewReservation` 跑测试
→ **V2 两项变红**（「管理员 0 条（拒绝也停推）」拿到 1 个 `UQJ5…` 调用；「落库 skipped」变成真实发送结果），
还原后 **26/26 全绿**、`grep -c "notifyAdmins(db" reviewReservation/index.js` = **0**。

**全量回归：181 项全绿**（remind 59 + cancel 32 + saveNotifyConfig 30 + listMyReservations 18 + listReviews 16 + **review 26**），
`TZ=UTC` 与 `TZ=Asia/Shanghai` 结果一致。

**部署与核对**：

| 函数 | ModTime | 状态 | 云端 CodeInfo 核对 |
|---|---|---|---|
| `reviewReservation` | 2026-09-12 00:54:57 | `Active` | ✅ 含「通过/拒绝都不再通知」注释 + `reason: 'admin push disabled on review'` |
| `reviewAllReservations` | 2026-09-12 00:55:03 | `Active` | ✅ 同上 |

> ⚠️ MCP `updateFunctionCode` **不会清除已注册的 `permissions.openapi`**（此前 `createReservation` 已实证）。本轮两函数此前已由
> DevTools 注册过云调用权限，故 MCP 覆盖代码后**订阅消息发送能力仍在**；若换成从未注册过的新函数，则仍需 DevTools「上传并部署」注册一次。

**无需前端改动**：本轮纯服务端行为变更，小程序端不必重新上传（§九 的 `util.js` / `hub.*` / `sessions.*` / `review.wxml` 仍需一次上传，那是另一件事）。

**真机复验要点**（下次有预约时）：
1. 管理员点「通过」→ 管理员侧**应零收到**；顾客应收到「预约成功」卡片（若顾客未授权则 `43101`）。
2. 管理员点「拒绝」→ **双方都零收到**。
3. 无论哪个动作，`reservations.adminNotifyReview` 都应是 `{skipped:true, decision:'approve'|'reject', reason:'admin push disabled on review'}`。
